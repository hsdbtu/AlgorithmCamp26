## **Başlangıç**

### **1. SORU**

`veri.txt` adlı bir dosya oluşturunuz.

* Dosyaya bir tam sayı ve bir ondalıklı sayı yazdırınız
* Dosyayı kapatınız

📌 `fprintf` kullanılmalıdır.

---

## **Orta**

### **1. SORU**

`veri.txt` adlı dosyadan:

* Bir `int`
* Bir `float`

değerini okuyup ekrana yazdıran programı yazınız.

📌 `fscanf` kullanılmalıdır.

---

### **2. SORU**

`notlar.txt` adlı bir dosyada:

* İlk satırda öğrenci sayısı
* Sonraki satırlarda notlar

bulunmaktadır.

Dosyadan bu bilgileri okuyup **notların ortalamasını** ekrana yazdırınız.

---

## **Zor**

### **1. SORU**

Bir `struct Ogrenci` tanımlayınız:

* `int no`
* `float ort`

Bu struct tipinden **bir değişkeni** binary dosyaya yazan ve
dosyadan geri okuyan programı yazınız.

📌 `fwrite` ve `fread` kullanılmalıdır.

---

### **2. SORU (Advanced)**

Dinamik bellek kullanarak:

* `n` elemanlı bir `struct Ogrenci` dizisi oluşturunuz
* Bu diziyi **binary dosyaya** yazınız
* Dosyadan tekrar okuyup ekrana yazdırınız

📌 `malloc + struct + File I/O` birlikte kullanılmalıdır.

---